# 代码审查发现与修复跟踪

**创建**：2026-07-24（成熟化改造后全面代码审查）
**方法**：5 路并行子系统审查（zEngine / zCommon / GameServer / MapServer / Global+Gateway）+ 逐条读码核实。

> **核实状态（重要，诚实说明）**：5 路审查 agent 中**仅 Global+Gateway 一路完整返回**；另 4 路（zEngine/zCommon/GameServer/MapServer）在进程退出时中断、恢复时又遇 403 未能取回结果。因此本清单中：
> - **`✓ 已核实`** = 我亲自读了相关代码确认为真、并已（或即将）修复。
> - **`⚠ 待核实`** = 初版由推断/模式匹配写入，**尚未逐行读码确认**，修复前必须先读码验证（可能有误报）。
> 修复严格遵循真相优先：**只修已核实项**，每项修复前都实际读码确认。`[x]` = 已修复。

> 修复门禁：每项修复后至少 `go build ./...` + 相关模块 vet 绿；并发类修复在 t1(Linux) 跑 `-race`；
> 影响跨服 live 路径的改动跑 E2E。

---

## 🔴 CRITICAL

### 安全
- [x] **SEC-1 ✓已核实已修 网关未鉴权即转发** `GatewayServer/client/message_handler.go:70`：跳过 TOKEN_VERIFY 直接发任意 protoId 转发给 GameServer。**已修**：AuthHandler 加 `IsAuthenticated(sessionID)`(查 connMgr AccountID!=0)，HandleMessage 转发前非认证会话拒绝+Close。
- [x] **SEC-2 ✓已核实已修 JWT 弱默认密钥** `GatewayServer/config.go:104`+`config.ini:54`：`"zMmoServerSecretKey"` 硬编码且不校验。**已修**：默认值改空、Validate 强制≥32、config.ini 改占位并注明须与 GlobalServer 一致+优先 env 注入。
- [x] **SEC-3 ✓已核实已修 `/shutdown` 无鉴权** `GlobalServer/http/http_service.go:229`：任何人单包关停全局服。**已修**：用真实 TCP peer RemoteAddr 判 loopback，仅本机可触发，远程返回 403。

### 崩溃 / 数据丢失（zCommon）
- [x] **INF-1 ✓已核实已修 MySQLConnector send-on-closed-channel panic** `db/connector/mysql_connector.go`：`isRunning` 裸 bool 竞争 + Query 与 Close 关 channel 竞态。**已修**：isRunning→atomic.Bool、Close 用 sync.Once、改 done channel（不 close queryCh）、worker/Query/cleaner 全 select on done。顺带修 INF-8(nil callback rows 泄漏)、OPT-3(DSN 明文日志脱敏)、OPT-8(cleaner 忙轮询→阻塞 ticker)。
- [x] **INF-2 ✓已核实已修 迁移 Inbox 幂等点错误** `crossserver/migration_manager.go handleMigrationData`：先 `TryAccept` 占用去重，再做 checksum/callback/反序列化校验，任一失败返 Success:false 但去重记录已留→重投命中"重复"分支误返 **Success:true**→源服删玩家、目标从未落地→**玩家丢失**。**已修**：InboxStore 新增 `Release(requestID)`（MemoryInbox 删记录减计数、SQLInbox `DELETE`），三条失败路径均先 `Release` 再返失败，使重投可真正重新处理；成功路径仍 `Ack`。新增 `TestMemoryInbox_ReleaseAllowsReaccept` 回归护栏，zCommon 全测试绿。
- [x] **INF-3 ✓已核实已修 etcd 租约 KeepAlive 绑 5s 请求 ctx** `discovery/service_discovery.go ensureLeaseAlive`：KeepAlive 绑传入的 5s 超时 ctx→5s 后保活流关闭、30s 租约到期(~T+35s)服务从 etcd 消失并反复重 grant churn。**已修**：结构体加服务级 `leaseCtx/leaseCancel`(构造时建、Close 时取消),KeepAlive 绑 leaseCtx(存活到 Close),Grant 仍用传入 ctx 一次性超时。**行为验证**：本地 4 服注册 t1 etcd,T+0 与 T+50s 查 `/services/` 前缀 game/gateway/global/map 均在(旧 bug 下 ~35s 应消失)→租约持续续约。

### 业务（GameServer / MapServer）
- [x] **MAP-1 ✓已核实已修 射程用真实距离比 range²** `combat/combat_system.go:137`、`skill/skill_manager.go:232`：`DistanceTo` 返真实距离(sqrt)却比 range²→range 3 实际允许 9（combat 可达，attack 已 E2E）。**已修**：两处改用 `DistanceSquared` 同量纲比较。
- [x] **GS-1 ✓已核实已修 Outbox 无界增长** `zCommon/consistency/memory_store.go:101`+`sql_store.go`：MarkSent 只置标志不删、MarkAcked 全项目无调用方→每次 move/attack 永留条目 OOM（可达）。**已修**：MarkSent/MarkAcked 直接删除条目（fire-and-forget 无 ack、已发送即终态）；内存+SQL 版一致，SQL 集成测试更新并绿。
- [~] **ECO-1 ⚠已核实但不修（不可达业务，原则性 descope）** `economy/shop.go:122`：BuyItem 只返回价格、不扣币不发货。**已核实：economy 全部函数无网络 protoId 入口、不可达**（同 2.4 结论）。完整修=扣币/发货/事务=堆经济玩法，违背"业务最少量"原则→**文档化为已知未完成**，若将来接入客户端须先做事务化购买（校验→扣币→扣库存→发货，失败回滚）。
- [~] **ECO-2 ⚠已核实但不修（不可达业务）** `economy/trade.go`：CompleteTrade 只搬货币不转物品。同 ECO-1，不可达业务，文档化不建。
- [x] **MAP-2 ✓已核实已修 对象无锁（数据竞争）** Player/Monster 内部字段（位置/血量/目标/状态）无同步，却被 100ms 游戏主循环(AI/战斗)、zNet 网络分发(进出/移动/攻击)、刷怪、死亡/重生定时多类 goroutine 并发读写；地图 m.mu 只护对象容器、不护对象字段。**已核实真实可达竞争**（攻击 tcp_service→AttackTarget→SetHealth 与 tick→AI monster.Move→SetPosition 并发）。**已修（用户拍板方案 A：单写者/Actor）**：新增 `maps/map_actor.go`，每张地图独占一条 goroutine 串行处理帧更新(postTick)与网络命令(Do)，五类变异入口全部串行化；`base_server` 主循环改 `PostTickAll` 投递。**验证**：t1 `-race ./maps/`×3 绿（新增回归护栏 `TestMapActor_ConcurrentMutation_NoRace`），真 E2E 绿（攻击 Damage=27 + AOI 推送 1220）。连带缓解 MAP-6/7/8。提交 da35f7a。
- [x] **MAP-3 ✓已核实已修 AI 巡逻在读锁内 time.Sleep(2s)** `ai/ai_manager.go updatePatrolBehavior`：到达巡逻点后 `time.Sleep(2s)`，而该函数由 100ms 主循环在 `AIManager.Update` 的 RLock 下调用→冻结整张地图 AI/Buff/玩家更新 2s 并阻塞 AI 增删（`behaviorPattern` 默认 "patrol"，路径默认可达）。**已修**：改非阻塞到期时间 `patrolPauseUntil`。提交 b580715。
- [x] **MAP-4 ✓已核实为否（Update 有被驱动）** `server/base_server.go startGameLoop`：100ms ticker 驱动 UpdateAllMapsAI/Buffs/Players/Skills/Events（现改为 PostTickAll→各地图 tick）。AI/Buff/玩家/技能/事件均被驱动，**非缺陷**。（Dungeon/Mirror 生命周期另有 dungeon_lifecycle 驱动；Auction/Shop 属不可达业务无需驱动。）

---

## 🟠 HIGH

- [x] **GS-2 ✓已核实已修 重连不刷新 sessionID** `player/login_service.go:199 Reconnect`：只 BindPlayer 不更新 Player.sessionID→重连后 AOI/推送(player_aoi_handler 用 p.sessionID)全发往死会话丢失。**已修**：Reconnect 里 GetPlayer 后 `p.SetSessionInfo(newSessionID, clientSender)`（与登录路径一致）。
- [x] **NET-1 ✓已核实已修 客户端真实掉线不设 closed→AutoReconnect 永不触发** `zNet/TcpClientSession.go receive`（审查中发现）。**已修+回归测试**（defer s.closed.Store(true)）。
- [x] **GS-3 ✓已核实已修 Player Actor 同步阻塞 1.5s 等跨服攻击** `player_net_handler.go handleNetMapAttack`→`map_service.go sendMapAttackRequest`：攻击在 Player actor goroutine 上同步等 MapServer 响应(最多 1.5s),期间该玩家邮箱(100)堆积、超限静默丢 move/AOI(人多时 AOI 密集尤甚)。**已修**:攻击只是"转发+回填响应"不改玩家本地状态→把跨服往返移到独立 goroutine,actor 立即返回;响应经 msg.Callback(缓冲1,不阻塞)回填,handler 侧仍 3s 超时,客户端语义不变。E2E 攻击 Damage=26 经新路径正常返回。
- [x] **GS-4 ✓已核实已修 非幂等重试重放 attack/enter** `map_service.go outboxRetryLoop`(1s tick 重发失败 pending) + MapServer 请求侧无去重 → 重放 attack=双倍伤害/enter=重复进图。**已修**:MapServer TCPService 加 `consistency.MemoryInbox`,`handleMapAttackRequest`/`handleMapEnterRequest` 头部按 `meta.RequestID` `TryAccept` 去重(重放直接丢弃,原始请求已应答);move/leave 幂等不去重。加 `inboxCleanupLoop`(每分钟 Cleanup 5min 窗口)限制增长(连带 INF-11 该表)。
- [x] **MAP-5 ✓已核实已修 对象 ID 用 time.Now().UnixNano()%1e9** `combat_handler.go:106/134/155`/`spawn_manager.go:144/160`：同纳秒/同窗口生成的怪/掉落物撞 ID 互相覆盖（战斗/刷怪可达）。**已修**：新增 `maps/object_id.go` 全局原子自增生成器（基数 3e9 避与玩家 ID 碰撞），5 处替换。
- [x] **MAP-6 ✓已核实已修 怪物移动绕过 AOI** `object/monster.go:301 Move`/`Chase`：直接改 `m.position` 从不调 AOI，AI 驱动的怪移动不发 AOI move 事件→客户端永远看不到怪移动（玩家移动经 `MoveObject` 有 AOI）。属 AOI 基础设施半失效。**已修**：在 `AIManager.Update` 单点拦截——快照每只怪 `update()` 前后位置，若变化则经新接口 `MoveEntityAOI`（Map 实现，调 `aoiManager.MoveEntity` 补发 AOI Move）同步，一处覆盖 Move/Chase/wander；因 AI 在地图 goroutine(tick) 上运行，触碰 AOI 安全。t1 `-race ./maps/` 绿。
- [x] **MAP-7 ✓已核实已修 AOI 通知在地图 goroutine 上同步网络发送** `net/service/aoi_push.go NotifyAOI`→`session.Send`：由 `handleAOIEvent` 在 actor goroutine 上同步调用，慢连接/发送缓冲满会卡住整张地图。**已修**：`NotifyAOI` 只把值化的 `aoiSendJob` 非阻塞投递到 `aoiSendCh`（满则丢弃，AOI 尽力而为），独立 `aoiSendLoop` goroutine 做查会话+marshal+pack+send，把网络 I/O 从地图 actor 剥离；生命周期挂 TCPService Start/Stop。E2E 实证解耦后 AOI 推送 ProtoId=1220 仍到达。
- [x] **MAP-8 ✓已核实已修 玩家离场不清 buff/spawn 计数** `map.go RemoveObject`：移除玩家不清 `buffManager.playerBuffs`（随进出无界增长）；`SpawnManager.RemoveObject` 零调用方→`currentCount` 只增不减、刷怪点达 maxCount 永久停摆（且旧"减第一个非零点"会错减满员点超刷）。**已修**：RemoveObject 对玩家调 `RemoveAllBuffs`；新增 `spawnedBy` 归属表精确回退计数、map.RemoveObject 对怪调 `spawnManager.RemoveObject`。连带修 MAP-2 遗留锁序隐患（原 spawn 持 sm.mu 跨阻塞 Do → 与新 RemoveObject 的 m.mu→sm.mu 成环死锁）：checkSpawnPoints 改两阶段（持锁建对象、锁外 Do 加入）。新增 `TestSpawnRemove_NoDeadlockNoRace`，t1 -race 绿。
- [x] **GW-1 ✓已核实已修 网关每 IP 连接计数只增不减** `client/connection/handler.go:56 OnClose`：OnClose 收到的 wrapper `GetClientIP()` 返回 ""→RemoveConnection("")，真实 IP 计数只增不减→达上限误封整 IP 自我 DoS。**已修**：OnClose 从 connMgr 按 sessionID 查回连接时存的真实 `ClientAddr` 再递减。
- [x] **GW-2 ✓已核实已修 网关代理并发写 tcpClient + 重连叠加** `proxy/game_server_proxy.go`：`tcpClient` 裸指针字段被 10s 轮询/etcd watch/SendToGameServer 三类 goroutine 并发读写→竞争；`connectToGameServer` 新建覆盖旧 client 不 Close→旧 AutoReconnect 监控 goroutine 泄漏+重复连接。**已修**：改 `atomic.Pointer[TcpClient]`，读用 Load、新建时 Swap 换入并 Close 旧（先换后连保 flush 语义）。提交 b580715。
- [x] **NET-2 ✓已核实已修 worker-pool 模式 Send 死锁** `zNet/TcpServerSession.go:135`：process() 是 sendChan 唯一消费者但仅在 !UseWorkerPool 时启动→开池(Gateway 默认)后 sendChan 无人消费、Send 填满永久阻塞。**已修**：process() 无条件启动（池模式 receiveChan 不喂、仅服务发送侧）。zNet 全测试+t1 -race 绿。
- [x] **NET-3 ✓已核实已修 序列号窗口 map 无界增长** `zNet/key_rotation.go:239`：先 Store windowStart 再 Load 回来致清理循环空转；且仅大跳变时清理、顺序流量永不清理→长连接无界增长。**已修**：先读旧 windowStart、每次前进都滑动裁剪。
- [x] **NET-4 ✓已核实已修 WS/UDP 会话 wg.Add 误用 + dispatcher 错误拆会话** `WebSocketServerSession.go`/`UdpServerSession.go`：同 TCP 已修问题——wg.Add(1) 写在 receive/process/heartbeat 内部而非 go 之前→Close 的 wg.Wait 可能计数 0 提前返回并与 goroutine 竞争；dispatcher 单包报错直接 `return`→拆掉整条会话 goroutine。**已修**：Start 在 go 之前 wg.Add；抽 `dispatchSafely`(隔离 error+recover panic，不 return，继续处理后续包)。zEngine build/vet 绿，t1 `-race ./zNet/...` 绿。
- [x] **NET-5 ✓已核实已修 满员时关服死锁** `zNet/TcpServer.go:135`：accept 满员空转分支不检查 closing→关服时 accept goroutine 不退、wg.Wait 挂起。**已修**：满员分支先检查 closing 并 break。

---

## 🟡 MEDIUM

- [x] **SEC-4 ✓已核实已修 反作弊按 sessionID 追踪+窗口失效+RecordError 未接入** `client/message_handler.go`/`security/anti_cheat.go`：①key=`client_<sessionID>`→客户端重连换 sid 即清零绕过；②速率检测用 `time.Since(LoginTime)<1min` 门控且 ActionCount 从不重置→登录一分钟后永久失效；③`RecordError` 定义了但无人调，错误率检测永远 0。**已修**：①改按 clientIP 追踪（连接期稳定、重连仍归并）；②ClientStats 加 WindowStart，每分钟滚动重置 ActionCount/ErrorCount，去掉坏的时间门控；③未认证发消息/转发失败时 `RecordError(clientIP,...)` 接入。build/vet 绿、E2E 无回归。
- [x] **SEC-5 ✓已核实已修 账号枚举+时序侧信道** `GlobalServer/handler/handler.go`：账号不存在→返回 `ERR_ACCOUNT_NOT_FOUND`/"账号不存在" 且不跑 bcrypt（快）；密码错误→跑 bcrypt（慢）+不同错误。**已修**：不存在分支跑一次假 bcrypt(cost=DefaultCost 与真实一致)抹平时序，并返回与密码错误完全相同的错误码/文案，杜绝枚举。
- [x] **SEC-6 ✓已核实已修 无账号级爆破锁定** GlobalServer 只有 IP 限速，换 IP 即可对单账号无限爆破。**已修**：新增 `accountLoginLockout`(内存,5 分钟窗口失败 5 次→锁 15 分钟,成功清零,机会性清理限增长)，登录入口先查锁定、失败(含账号不存在)计数、成功清零。多实例需换 Redis(已注明)。新增 `TestAccountLockout`/`_WindowReset`。
- [x] **GW-3 ✓已核实已修 多端登录无踢人+旧连接断开误删新映射** `client/connection/manager.go`：SetAccountInfo 覆盖 accountMap 不踢旧会话（双端在线）；旧会话断开时 RemoveSession 无条件 `accountMap.Delete`，而该映射已被新端覆盖→误删新会话映射（新端按账号推送/查找全失效）。**已修**：SetAccountInfo 先落新映射再经注入的 kickFn 关闭旧连接；RemoveSession 仅当本会话仍是该账号当前会话（`accountMap[acc]==sessionID`）时才清账号+名称映射。Service 注入 kickFn=`netServer.GetSession(sid).Close()`。E2E 登录/进图/攻击无回归。
- [x] **GW-4 ✓已核实已修 服务器列表缓存无锁竞争** `GlobalServer/gameserverlist/server_list.go GetAllServerFullInfos`：`serverListCache`/`serverListCacheTime` 裸字段，方法内读检查(209)+重建替换(252)无锁，HTTP 处理器并发调用竞争 slice 头/时间戳。**已修**：加 `cacheMu sync.Mutex`，读检查与重建全在锁内串行（重建替换整条 slice 不就地改，返回的 slice 头指向稳定数组）。新增 `TestGetAllServerFullInfos_ConcurrentNoRace`(短 TTL 强制重建 vs 8 goroutine 并发)，t1 `-race` 绿。
- [x] **INF-4 ✓已核实已修 2PC Commit 部分失败不回滚+ctx.Done break 只跳 select** `consistency/transaction.go Commit`：`select{case <-ctx.Done(): break}` 的 break 只跳出 select 而非 for（根本没停）；部分参与者提交成功、部分失败时误标 Aborted（暗示已回滚，实则已提交者未回滚）。**已修**：Prepared 后提交阶段对失败参与者有界重试(3 次)；仍失败则置新增的 `TransactionStateInconsistent` 态并告警等人工/补偿介入（不误标 Aborted），不再中途 break 放弃剩余参与者。新增 `TestCommit_RetryAndInconsistent`（瞬时失败重试成功→Committed；永久失败→Inconsistent 且其余参与者仍提交）。
- [x] **INF-5 ✓已核实已修 MemoryInbox.TryAccept 非原子** `consistency/memory_store.go:271`：Load-then-Store 非原子→两并发同 requestID 都判不存在、都 Store、都返 true→去重失效消息处理两次。**已修**：改原子 `LoadOrStore`（loaded 即重复返 false）。t1 -race 绿。
- [ ] **INF-6 迁移 Record/Transaction 结构体字段跨 goroutine 无锁** `migration_manager.go`/`transaction.go`：Vote 与 Prepare 并发写 map→panic。修：加结构体锁。
- [ ] **INF-7 MySQLConnector Execute 每次起 goroutine 无背压+Close 不等待** `mysql_connector.go:308`。修：走 worker 池或信号量+纳入 wg。
- [x] **INF-8 ✓已核实已修 queryWorker callback nil 时 rows 泄漏连接** `mysql_connector.go`。**已修**：无回调时也 `rows.Close()`。
- [ ] **INF-9 Mongo 传 nil ctx（新驱动 panic）+ rows.Err() 未检查** `mongo_connector.go`/各 DAO。修：WithTimeout；errors.Is(mongo.ErrNoDocuments)；查 rows.Err()。
- [x] **INF-10 ✓已核实已修 SQLOutbox MarkAttempt 非原子+ListRetryable 无 ORDER BY** `consistency/sql_store.go`：①MarkAttempt 走 SELECT attempts→Go 自增→UPDATE 写回，并发同 requestID 丢更新（计数少加）；②query 带 LIMIT 无 ORDER BY→返回顺序不定，到期消息可能被截断在窗口外饿死永不重投。**已修**：①计数改 SQL 原子 `attempts=attempts+1`（读旧值仅用于近似退避）；②query 加 orderBy 参数，ListRetryable 按 `next_retry_at IS NULL DESC, next_retry_at ASC, created_at ASC`、ListPending/DeadLetters 按时间升序。t1 真 MySQL 集成测试（含 MarkAttempt/退避/ListRetryable）绿。
- [x] **MAP-9 ✓已核实已修 伤害/暴击用 time.Now()%1000 当随机** `combat_system.go:48,178`：可卡时序稳定刷暴击、同毫秒连击同值（combat 可达）。**已修**：改用 `math/rand/v2`（goroutine 安全）。
- [ ] **MAP-10 死亡/重生用裸 goroutine 无取消+绕过 AOI** `combat_handler.go:75,132`。修：纳入 tick 定时任务+存活校验。
- [ ] **MAP-11 副本/镜像/跨服地图泄漏** `mirror_map_manager.go`/`dungeon_lifecycle.go`/`cross_server_entry.go`：不 Cleanup 底层 Map/不停 spawnLoop/跨服每次新建地图。修：销毁经 MapManager 统一 Cleanup+Stop；跨服走镜像去重。
- [ ] **MAP-12 Buff 无叠加上限+只支持玩家** `buff_manager.go:104,49`：怪不能中毒。修：读配置 MaxStack；容器改 ObjectID 键。
- [ ] **MAP-13 ApplyDamage 忽略护盾/无敌/减伤+int64→int32 截断** `combat_system.go:109`。修：伤害前查无敌/护盾+减伤；用指针传 result。
- [ ] **MAP-14 拍卖无托管/可重复结算/并发出价竞态** `economy/auction.go`。修：出价托管+结算状态守卫+每拍卖锁+失败回滚+物品交割。
- [x] **NET-6 ✓已核实已修 客户端密钥轮换只更 keyID 不更 key** `zNet/TcpClientSession.go`：轮换通知只带新 KeyID、不含新密钥材料，客户端 `handleKeyRotationNotify` 仅 `Store(KeyID)`→keyID 与实际密钥失配、加解密全乱（整个特性缺安全密钥下发信道）。**已修（暂不启用该特性）**：客户端收到通知仅告警不应用（保持握手稳定密钥）；服务端即便配置 `EnableKeyRotation` 也只告警不启动自动轮换（不再换 key 制造失配）。待实现安全再密钥（DH 再交换下发新 key）后恢复。zNet 全测试绿。
- [x] **NET-7 ✓已核实已修 zServer 状态监听器用栈地址作 key** `zServer/state.go`：`Store(uintptr(unsafe.Pointer(&listener)))` 用参数栈地址作 key——Add 与 Remove 的 `&listener` 地址不同→永远删不掉，栈地址复用还可能相互覆盖。**已修**：`stateListeners` 键改 `uint64`，加 `listenerSeq atomic.Uint64`，`AddStateChangeListener` 返回自增句柄，`RemoveStateChangeListener(handle)` 按句柄删。（原 API 全项目无调用方，改签名安全。）新增 `TestStateChangeListener_HandleAddRemove`，t1 `-race ./zServer/` 绿。
- [x] **NET-8 ✓已核实已修 TcpClient.Connect 重复调用泄漏旧会话** `zNet/TcpClient.go`：Connect 无条件新建会话并 `session.Store(s)`，旧会话(重连时)不 Close→其 goroutine+底层 conn 泄漏。**已修**：改 `session.Swap(s)` 原子换入并 `old.Close()`。t1 `-race ./zNet/`(含重连用例)绿。
- [x] **INF-11 ✓已核实已修（可达项）各种去重/缓存表无界增长** ①GameServer MapService `inbox` 每次跨服攻击响应存 requestID 从不清（MapServer 侧 GS-4 已修，GameServer 侧漏）→ outboxRetryLoop 加每分钟 `inbox.Cleanup(5min)`；②zNet DDoS `ConnectionLimiter/PacketLimiter.records` 每见一 IP 存 *ipRecord 从不删→加 `maybeCleanupIPRecords`（每 window 至多清一次空闲 IP 条目，无需独立 goroutine）。核实不属实/已自清的：TimeoutManager（ctx.Done 自删）、TrafficLimiter（窗口重置清空）、IPBlacklist（惰性过期删）。交易/拍卖属不可达业务（ECO descope），skillCooldowns 由怪技能数有界。

---

## 🟢 LOW / 优化

- [x] **OPT-1 ✓已核实已修 generateAESKey 忽略 rand.Read 错误** `zNet/key_rotation.go:49`：熵源失败得全零密钥。**已修**：检查 error 并 panic。
- [x] **OPT-2 ✓已核实已修 GetObjectsInRange 全量 O(n) 扫描+每对象 sqrt** `maps/map.go`：战斗/技能/AI 选目标热路径每次扫全场并逐对象开方。**已修**：抽 `collectInRange`——半径 ≤ 单格尺寸(50)时用 `aoiManager.GetSurroundingEntities` 把候选缩到周边 3x3 格，半径超格则回退全量扫描保正确；比较改平方距离省 sqrt。`GetObjectsInRange`/`getObjectsInRangeByType`(含 GetPlayers/Monsters/NPCsInRange)统一走之。新增 `TestGetObjectsInRange_MatchesBruteForce`(6 组小/边界/大半径与朴素全扫结果逐一比对)，t1 -race 绿，E2E 无回归。
- [x] **OPT-3 ✓已核实已修 明文密码 DSN 入日志** `mysql_connector.go:151`。**已修**：连接信息脱敏，不打印含密码 DSN。
- [x] **OPT-4 ✓已核实已修 CORS 全开** `GlobalServer/http/http_service.go`：`middleware.CORS()` 默认放行所有来源(*)，任意网站可跨域打登录 API。**已修**：仅当经 env `ZMMO_HTTP_ALLOW_ORIGINS`(逗号分隔)显式配置白名单才放行对应来源；未配置则不加 CORS 中间件（不下发 Access-Control-Allow-Origin，浏览器按同源策略拒绝跨域；原生客户端不发 Origin 不受影响）。
- [ ] **OPT-5 zMetrics 跨类型同名注册 MustRegister panic** `zMetrics/manager.go:70`。修：注册前查所有类型重名。
- [ ] **OPT-6 A* 无 decrease-key（次优路径）** `zNavMap/AStar.go:130`。修：heap.Fix + 弹出时判目标。
- [x] **OPT-7 ✓已核实已修 刷出/重生怪等级恒为 1** `spawn_manager.go`/`combat_handler.go`：`NewMonster(..., 1)` 硬编码等级→数值全错。**已修**：新增 `monsterConfigLevel(monsterConfigID)` 读 `lootSystem.GetMonsterConfig().Level`(models.Monster 已有 Level 字段，非新增业务，配置缺失回退 1)，刷怪与重生两处均改用之。
- [x] **OPT-8 ✓已核实已修 忙轮询** cacheCleaner/metricsPrinter。**已修**：改 `select{case<-ticker.C;case<-c.done}` 阻塞式，去 default 忙等（随 INF-1 一并修）。
- [x] **OPT-9 ✓已核实已修 DedupStore 二次 close panic + expire 过小 NewTicker(0) panic** `request/request.go`：Stop 二次 `close(stopCh)` panic；`NewTicker(expire/2)` 当 expire<2ns 时为 0 → panic。**已修**：Stop 用 `stopOnce`；cleanupLoop 间隔下限保护（≤0 取 1s）。
- [x] **OPT-10 ✓已核实已修 TableManager 单例双检竞争** `config/tables/table_manager.go`：`if GlobalTableManager==nil` 无锁读全局、同时 Once.Do 内写它 → 竞争。**已修**：去掉双检，无条件 `tableOnce.Do`（线程安全且首次后近零成本）。
- [ ] **OPT-11 zServer isRunning 恒 false 死 API + Send 未连接返回 ErrWriteToConnected 语义错**。修：置 true / 自定义错误。
- [ ] **OPT-12 lifecycle Manager.mu 声明未用+对象状态无锁** `lifecycle/manager.go`。修：加锁或去除。
- [x] **OPT-13 ✓已核实已修 zUtil zMap.Store 计数非原子** `zUtil/zMap/map.go`：`Load(!ok)→count.Add(1)→Store` 检查-再动作，并发 Store 同一新键各 +1 → Len() 多计。**已修**：改原子 `sMap.Swap` 一步判存在维护计数。新增并发同键/异键计数测试，t1 -race 绿。
- [ ] **OPT-14 async 日志缓冲满同步直写致乱序+Close 后 Write 不检查** `zLog/async_writer.go:97`。修：满时也入队或丢弃计数；Close 后拒写。

---

## 修复批次计划
1. **批 1（小改动/高收益/低风险）**：NET-1(已修) / SEC-1 / SEC-2 / SEC-3 / INF-1 / GS-2 / NET-5 / OPT-1 / OPT-3。
2. **批 2（业务正确性/经济安全）**：MAP-1 / MAP-2 / MAP-3 / ECO-1 / ECO-2 / MAP-9 / MAP-13。
3. **批 3（可靠性闭环）**：GS-1 / GS-3 / GS-4 / INF-2 / INF-3 / INF-5 / INF-6 / MAP-4。
4. **批 4（其余 HIGH/MEDIUM/优化）**：逐条清。
