# AGENTS.md

面向任意 AI 助手 / 编辑器 / 人类贡献者的仓库入口（跨工具通用 `AGENTS.md` 标准）。
换任何 AI 或编辑器都从这里开始。**细节一律以下列文档为准，本文不复制内容（避免多处漂移）。**

## 先分清你在做哪类工作

- **理解架构 / 在框架上开发游戏功能**（加系统、玩法、地图、协议、持久数据）
  → 先读 [`docs/架构.md`](docs/架构.md)，再看 [`docs/游戏开发指南.md`](docs/游戏开发指南.md)（**七条架构铁律** + 加功能配方）。
  Claude Code 技能：`zmmo-game-dev`。
- **开发 / 改进框架本身**（引擎、可靠性、契约、性能、重构）
  → [`docs/开发约定与工作流.md`](docs/开发约定与工作流.md)（工作环 + 验证纪律）。
  Claude Code 技能：`zmmo-dev`。

两者共用同一套验证纪律。开发游戏功能前，务必逐条对照《游戏开发指南》的七条架构铁律
（权威边界 / MAP-2 单写者 / AOI 单一权威 / 分线透明 / realm 隔离 / 契约先行 / 幂等崩溃安全）——它们对应过真实缺陷。

## 开工前必读（按序）

1. [`docs/架构.md`](docs/架构.md) — 建立架构心智（四进程 / realm / 权威分层 / 单写者 / AOI）。
2. 按任务：加功能看 [`docs/游戏开发指南.md`](docs/游戏开发指南.md)；改框架看 [`docs/开发约定与工作流.md`](docs/开发约定与工作流.md)。
3. [`docs/协议契约.md`](docs/协议契约.md) — 改跨服协议前必读（唯一编解码源 `zCommon/crossserver/codec.go`）。

## 通用铁律（细节见上述文档）

- **自评不可信**：每个"完成"必须挂一个通过的测试或一次跨进程真 E2E，不凭"看起来对"。
- **一次一块做透**：编译绿 + 测试绿 + E2E 绿 + 文档更新，才开下一块。
- **未跑到的路径必藏 bug**：优先怀疑"实现了但从没真跑过"的代码。
- **多 module 无 `go.work`**：用 `go -C <dir>` 逐模块；全量门禁 `scripts/ci-check.ps1`；判成败用退出码，别用 `2>&1`；gopls 跨模块报红是假阳性，以 `go build` 为准；并发类 `-race` 须在有 gcc 的环境跑。
- **版本 `0.0.X` 递增，成熟前不发 1.0**；业务层只做最少量、以架构为重。

代码位置：`zEngine`（引擎）、`zMmoServer`（分布式 MMO 服务端）、`zUtil`（工具库）——三个独立仓库，go.mod `replace` 指向本地同级目录。
