# AGENTS.md

面向任意 AI 助手 / 编辑器 / 人类贡献者的仓库入口。此文件是跨工具通用约定（`AGENTS.md` 标准），
换任何 AI 或编辑器都从这里开始。

## 唯一真相源

**开发方法、验证纪律、版本与文档约定、环境坑，全部见 [`docs/开发约定与工作流.md`](docs/开发约定与工作流.md)。**
本文只放最关键的几条；细节一律以那份文档为准（不在多处复制，避免漂移）。

## 开工前必读（按序）

1. `docs/成熟化改造-执行计划.md` — 进度台账，**勾选框 = 进度真相**。
2. `docs/框架成熟化改造方案.md` — 战略 / 三项已定决策 / 五阶段 / 验收标准。
3. `docs/协议契约.md` — 跨服 Game↔Map 契约（唯一编解码源 `zCommon/crossserver/codec.go`）。
4. `docs/开发约定与工作流.md` — 怎么干、怎么验证、踩过的坑。

## 铁律（细节见开发约定文档）

- **自评不可信**：每个"完成"必须挂一个通过的测试或一次跨进程真 E2E，不凭"看起来对"。
- **一次一块做透**：编译绿 + 测试绿 + E2E 绿 + 台账更新，才开下一块。
- **未跑到的路径必藏 bug**：优先怀疑"实现了但从没真跑过"的代码。
- **多 module 无 `go.work`**：用 `go -C <dir>` 逐模块；全量门禁 `scripts/ci-check.ps1`；判成败用退出码，别用 `2>&1`；gopls 跨模块报红是假阳性，以 `go build` 为准。
- **版本 `0.0.X` 递增，成熟前不发 1.0**；业务层只做最少量、以架构为重。

代码位置：`zEngine`（引擎）、`zMmoServer`（分布式 MMO 服务端）、`zUtil`（工具库）。
