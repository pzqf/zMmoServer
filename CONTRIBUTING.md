# 贡献指南

感谢参与 zMmoServer / zEngine。本项目的目标是**上线级 + 可复用的开源框架**，因此对"完成"的标准很高。

## 唯一真相源

开发方法、验证纪律、版本与文档约定、环境坑，**全部见 [`docs/开发约定与工作流.md`](docs/开发约定与工作流.md)**。
本文只提炼贡献者最需要先知道的几条；细节以那份文档为准（不在多处复制）。

## 核心原则

1. **真相优先**：任何"完成"必须由**通过的测试**或**跨进程真 E2E**（能断言下游真实状态）背书，不凭"看起来对"。
2. **一次一块做透**：编译绿 + 测试绿 + E2E 绿 + 文档/台账更新，才算完，再开下一块。
3. **同义原语单一定义**：领域类型只在 `zCommon/game` 定义，各 server 引用，杜绝静默分歧。
4. **契约集中**：跨服 protoId/消息/编码只经唯一源 `zCommon/crossserver/codec.go`，改动同步更新 `docs/协议契约.md` + 契约测试。
5. **业务层最少量**：只做够测试架构所需的业务，重点是整体架构。

## 提交前自检

- [ ] 相关模块 `go build ./...` 绿；改了底层（zEngine/zCommon）则上层模块也 build 绿。
- [ ] `scripts/ci-check.ps1` 全模块 build+vet 绿。
- [ ] 新增/改动逻辑有测试守护；跨服/网络/持久化类跑真 E2E 或真依赖集成测试。
- [ ] 在 `docs/成熟化改造-执行计划.md` 台账勾选并写清证据（测试名 / E2E 结果）；里程碑追加变更记录（版本 `0.0.X`）。
- [ ] 契约有变则同步 `docs/协议契约.md` + 契约测试。
- [ ] 临时探针/脚本已清理。

## 工程须知

- 多 module（各自 `go.mod` + `replace`，**无 `go.work`**）：用 `go -C <dir>` 逐模块；编辑器 gopls 跨模块报"undefined/could not import"是假阳性，以 `go build` 为准。
- 版本号 `0.0.X` 递增，成熟前不发 1.0。
- `-race` 硬门禁须在 CI/Linux 跑（Windows 本机无 C 编译器会自动降级）。

## 许可证

贡献即同意以 [MIT License](LICENSE) 授权。
